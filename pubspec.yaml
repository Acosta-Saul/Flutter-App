name: flutter_app
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'


dependencies:
  flutter:
    sdk: flutter
  
  google_ml_kit: ^0.12.0 # Cambié a una versión compatible
  image_picker: ^0.8.4+4 # Actualicé a una versión más reciente
  firebase_core: ^2.8.0
  cloud_firestore: ^4.4.5


  cupertino_icons: ^1.0.2
  qr_flutter: ^4.0.0

dev_dependencies:
  change_app_package_name: ^1.0.0
  flutter_test:
    sdk: flutter


  flutter_lints: ^1.0.0


flutter:


  uses-material-design: true

  assets:
    - assets/Logo.PNG